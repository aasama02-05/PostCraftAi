## Packages
framer-motion | Essential for smooth page transitions, tab animations, and staggering result reveals
date-fns | Used for beautiful human-readable timestamps in the history tab
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes safely

## Notes
The application expects standard REST API functionality.
Authentication is assumed to be handled at the network level or omitted for this tool.
No external API keys are required from the user as it calls the internal Replit backend.
